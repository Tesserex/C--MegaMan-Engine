<UserControl x:Class="MegaMan.Editor.Controls.SpriteEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0" Grid.Row="0">
            <Image x:Name="previewImage" Source="{Binding PreviewImage}" DockPanel.Dock="Top" Height="{Binding PreviewWidth}" Width="{Binding PreviewHeight}" />
            <StackPanel Orientation="Horizontal">
                
            </StackPanel>
        </DockPanel>
        <DockPanel Grid.Column="1" Grid.Row="0">
            <Canvas DockPanel.Dock="Top">
                <Image Canvas.Top="0" Canvas.Left="0" Source="{Binding SheetImageSource}" />
                <Canvas Canvas.Top="0" Canvas.Left="0" x:Name="sheetCanvasOverlay" />
            </Canvas>
        </DockPanel>
    </Grid>
</UserControl>
