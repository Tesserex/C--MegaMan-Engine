<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="MegaMan.Editor.MainWindow"
        xmlns:avalonDock="http://avalondock.codeplex.com"
        xmlns:c="clr-namespace:MegaMan.Editor.Controls"
        Title="Mega Man Project Editor" Height="700" Width="1100">
    <Window.Resources>
        <avalonDock:VS2010Theme x:Key="VS2010Theme" />
        <avalonDock:ExpressionLightTheme x:Key="ExpressionLightTheme" />
        <avalonDock:ExpressionDarkTheme x:Key="ExpressionDarkTheme" />
    </Window.Resources>
    
    <Window.CommandBindings>
        <CommandBinding Command="New" Executed="NewProject" CanExecute="CanExecuteTrue" />
        <CommandBinding Command="Open" Executed="OpenProject" CanExecute="CanExecuteTrue" />
    </Window.CommandBindings>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New" Command="New">
                    <MenuItem.Icon>
                        <Image Source="/Resources/NewDocumentHS.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Open" Command="Open">
                    <MenuItem.Icon>
                        <Image Source="/Resources/openHS.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Recent"/>
                <Separator />
                <MenuItem Header="_Save" Command="Save">
                    <MenuItem.Icon>
                        <Image Source="/Resources/saveHS.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Header="_Close" Command="Close"/>
                <MenuItem Header="_Quit"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="Undo" Command="Undo">
                    <MenuItem.Icon>
                        <Image Source="/Resources/Undo.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Redo" Command="Redo">
                    <MenuItem.Icon>
                        <Image Source="/Resources/Redo.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Header="Copy" Command="Copy">
                    <MenuItem.Icon>
                        <Image Source="/Resources/CopyHS.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Paste" Command="Paste">
                    <MenuItem.Icon>
                        <Image Source="/Resources/PasteHS.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                    
                </MenuItem>
                <MenuItem Header="Paste as Brush">
                    <MenuItem.Icon>
                        <Image Source="/Resources/PasteBrush.png" SnapsToDevicePixels="True" Stretch="Uniform" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Tiles"/>
                <MenuItem Header="Blocking"/>
                <MenuItem Header="Entities"/>
                <MenuItem Header="Grid"/>
                <MenuItem Header="Animate"/>
                <MenuItem Header="Joins"/>
            </MenuItem>
            <MenuItem Header="_Stage">
                <MenuItem Header="New Stage"/>
                <MenuItem Header="Properties"/>
                <MenuItem Header="Stage Select..."/>
            </MenuItem>
            <MenuItem Header="_Screen">
                <MenuItem Header="Split Screen"/>
                <MenuItem Header="Merge Screen"/>
                <Separator />
                <MenuItem Header="Clear Screen"/>
            </MenuItem>
            <MenuItem Header="_Windows">
                <MenuItem Header="Brushes"/>
                <MenuItem Header="Project"/>
                <MenuItem Header="History"/>
                <MenuItem Header="Entities"/>
            </MenuItem>
            <MenuItem Header="_Test">
                <MenuItem Header="Run Project"/>
                <MenuItem Header="Run Stage"/>
                <Separator />
                <MenuItem Header="Set Engine Path"/>
            </MenuItem>
        </Menu>

        <avalonDock:DockingManager x:Name="dockingManager" Grid.Row="1" Theme="{StaticResource ExpressionLightTheme}" DocumentPaneControlStyle="{StaticResource HeadlessDocumentPaneControlStyle}">
            <avalonDock:LayoutRoot>
                <avalonDock:LayoutPanel Orientation="Horizontal">
                    <avalonDock:LayoutAnchorablePane DockWidth="200">
                        <avalonDock:LayoutAnchorable Title="Project">
                            <c:ProjectTree x:Name="projectTree"></c:ProjectTree>
                        </avalonDock:LayoutAnchorable>
                    </avalonDock:LayoutAnchorablePane>
                    <avalonDock:LayoutDocumentPane>
                        <avalonDock:LayoutDocument>
                            <TabControl>
                                <TabControl.Resources>
                                    <Style TargetType="{x:Type TabPanel}">
                                        <Setter Property="HorizontalAlignment" Value="Right" />
                                    </Style>
                                    <Style TargetType="{x:Type TabItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type TabItem}">
                                                    <ContentPresenter x:Name="ContentSite"
                                                     VerticalAlignment="Center"
                                                     HorizontalAlignment="Center"
                                                     ContentSource="Header"
                                                     Margin="30,10,30,10"
													 TextBlock.FontSize="18"
													 TextBlock.FontFamily="Megaman 2" />
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="DarkGoldenrod"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="False">
                                                            <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="Black"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>

                                    </Style>
                                    <Style TargetType="TabControl">
                                        <Setter Property="BorderThickness" Value="0,0,0,0"></Setter>
                                    </Style>
                                </TabControl.Resources>
                                <TabItem>
                                    <TabItem.Header>
                                        Layout
                                    </TabItem.Header>
                                    <c:StageLayoutControl x:Name="stageLayoutControl"></c:StageLayoutControl>
                                </TabItem>
                                <TabItem>
                                    <TabItem.Header>
                                        Tiling
                                    </TabItem.Header>
                                    <DockPanel>
                                        <c:TileStrip x:Name="tileStrip" DockPanel.Dock="Top"></c:TileStrip>
                                        <c:StageTileControl x:Name="stageTileControl"></c:StageTileControl>
                                    </DockPanel>
                                </TabItem>
                                <TabItem>
                                    <TabItem.Header>
                                        Entities
                                    </TabItem.Header>
                                </TabItem>
                            </TabControl>
                        </avalonDock:LayoutDocument>
                    </avalonDock:LayoutDocumentPane>
                </avalonDock:LayoutPanel>
            </avalonDock:LayoutRoot>
        </avalonDock:DockingManager>

        
    </Grid>
</Window>
